enum class DrawMode {
    POINTS,
    LINES,
    LINE_STRIP,
    LINE_LOOP,
    TRIANGLES,
    TRIANGLE_STRIP,
    TRIANGLE_FAN
}


Draw(
        const DrawMode          mode,
        const vector<Vec3>&     vertices,           //declare vector<Vec2> as static?
        const vector<size_t>&   indices, 
        const Texture&          texture
        const Mat3&             modelViewMatrix,
        const Mat3&             projectionMatrix,
        FrameBuffer&            buffer
)